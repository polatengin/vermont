name: Test Alpine Runner
on: [push]

jobs:
  alpine-test:
    runs-on: alpine-latest
    steps:
      - name: Test Alpine
        run: |
          echo "Running on Alpine Linux"
          cat /etc/os-release
          which sh
          which bash || echo "bash not available, using sh"
